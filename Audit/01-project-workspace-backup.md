# 01 — Proyecto, Workspace, Backup/Restore

Ver leyenda de estado en [00-INDEX.md](00-INDEX.md).

## `project.py`

| Tool | Acción EPLAN | Parámetros MCP | Estado | Notas |
|---|---|---|---|---|
| `open_project` | `ProjectOpen` | `project_path` (req), `open_mode` (`Standard`\|`ReadOnly`\|`Exclusive`) | ✅ | Confirmado 2026-07-13 con `project_path="Nuevo proyecto.elk"`, sin `open_mode`. Resultado: `{"success": true, "parameters": {"PROJECT": "...", "Project": "..."}}` (nótese que devuelve la clave duplicada en dos capitalizaciones distintas, `PROJECT` y `Project`). **Gap de contexto encontrado y corregido**: el docstring original no advertía a la LLM contra adivinar/reutilizar una ruta de proyecto vista previamente en la conversación; se añadió una instrucción explícita en `project.py` (`open_project`) para que siempre pregunte al usuario la ruta salvo que ya se la haya dado en ese turno. |
| `close_project` | `XPrjActionProjectClose` | — | ✅ | Confirmado 2026-07-13. **Hallazgo clave**: la acción no acepta nombre/ruta de proyecto — cierra el que tenga el **foco de ventana** en la GUI de EPLAN en ese instante (el último que el usuario clicó), no uno elegible por API. Se probó `selectionset /TYPE:PROJECTS` (plural) y `/TYPE:PROJECT` (singular) vía `execute_custom_script` esperando que `PROJECTS` listara todos los proyectos abiertos: en la práctica **siempre devuelven el mismo único valor**, reflejando solo el proyecto con foco actual — cambia con solo hacer clic en otra ventana, dos ejecuciones seguidas del mismo script devolvieron dos proyectos distintos sin que la LLM hiciera nada entremedias. No existe ninguna acción conocida que enumere "todos los proyectos abiertos". Un intento de cerrar `Nuevo proyecto.elk` explícitamente cerró `Macros_7C0x_v.7.13.00.elk` en su lugar porque ese tenía el foco. **Gap corregido**: docstring de `close_project` documenta esta limitación explícitamente y instruye a la LLM a pedir confirmación de cuál tiene el foco antes de cerrar si hay más de un proyecto abierto. No se verificó qué pasa con cambios sin guardar — sigue siendo una incógnita, ver `llm.md` §7. |
| `project_management` | `projectmanagement` | `type` (req: `READPROJECTINFO`\|`PUBLISHSMARTPRODUCTION`\|`CREATESNAPSHOTCOPY`\|`EXPORTPROPERTYPLACEMENTSSCHEMAS`\|`IMPORTPROPERTYPLACEMENTSSCHEMAS`\|`REORGANIZE`\|`CORRECTPROJECTITEMS`\|`LOADDIRECTORY`), `project_name`, `filename`, `scheme`, `overwrite`, `extended_mode`, `projects_directory`, `scan_subdirectories` | ✅ | Confirmado 2026-07-13, cerrado. **`READPROJECTINFO` NO es de solo lectura** (a pesar del nombre) — la doc oficial dice que "carga info de un XML *hacia* el proyecto", es un import y requiere `filename` (XML de origen). Probado sin `filename` → `{"success": false}` sin mensaje (gap de excepciones tragadas, ver `TODO.md`). **Reprobado con `filename` apuntando a un XML del formato equivocado** (`PPSchemas.xml`, que es `PropertyView`, no `ProjectInfo`) → devolvió `{"success": true}`. Esto es un hallazgo más grave: `success:true` **no garantiza que la operación haya hecho algo válido** — con un XML de esquema incorrecto, pudo haber importado datos parciales/basura o no haber hecho nada, y de cualquier forma el wrapper reporta éxito igual. No verificado qué cambió realmente en el proyecto tras esto (no había snapshot previo con el que comparar) — **posible riesgo de corrupción silenciosa de datos si se usa con un XML mal formado sin darse cuenta**. **`CREATESNAPSHOTCOPY` = crear una copia completa de un proyecto.** Uso real: `type="CREATESNAPSHOTCOPY"`, `project_name=<ruta .elk origen>`, `filename=<ruta destino>` — **la extensión de `filename` es irrelevante, EPLAN siempre añade `.elk`+`.edb` propios sobre el nombre base**, así que no hace falta (ni tiene sentido) ponerle ninguna extensión; mejor pasar solo el nombre base, ej. `"test"`. Confirmado con `filename="test.xml"` (extensión de prueba, innecesaria) → generó `test.xml.elk` (36 bytes, stub) + `test.xml.edb` (carpeta, base de datos real) — la `.xml` quedó absorbida dentro del nombre base, no descartada. `scheme` sigue sin forma de listarse sin GUI — mismo problema que `CONFIGSCHEME` en otras acciones. **`EXPORTPROPERTYPLACEMENTSSCHEMAS`**: confirmado, exporta de verdad a un XML real (aquí sí la extensión `.xml` es correcta y se respeta, a diferencia de `CREATESNAPSHOTCOPY`). Genuinamente de solo lectura sobre el proyecto. Probado con `filename="PPSchemas.xml"` → generó XML de 69 KB válido (`<EplanPxfRoot Type="PropertyView" ... SourceProject="...">`, 31 objetos). **Emparejamiento real de ida/vuelta**: `EXPORTPROPERTYPLACEMENTSSCHEMAS` ↔ `IMPORTPROPERTYPLACEMENTSSCHEMAS` — mismo esquema (`Type="PropertyView"`), no confundir con `READPROJECTINFO` que espera un XML de "ProjectInfo" distinto. Si al usuario le falta el XML de property placement schemes, se genera con `EXPORTPROPERTYPLACEMENTSSCHEMAS` sobre cualquier proyecto que ya lo tenga configurado. **`IMPORTPROPERTYPLACEMENTSSCHEMAS`**: confirmado con ciclo completo real — se exportó `PPSchemas.xml` desde `Macros_7C0x` y se importó con éxito en `test_mcp.elk` (`overwrite=1`). `{"success": true}` con formato correcto — a diferencia del falso positivo de `READPROJECTINFO`, aquí sí hay confianza en el resultado porque el ciclo export→import usa el mismo esquema. Nota: dentro de `test_mcp.edb` existe un `ProjectInfo.xml` real (47.8 KB) — confirmado como el `filename` correcto para `READPROJECTINFO`: cada proyecto trae su propio `<project>.edb\ProjectInfo.xml`, y esa acción lo importa hacia el proyecto destino (mismo archivo como refresh propio, o el de otro proyecto para traer su info). Documentado en el docstring de `project_management`. **`REORGANIZE`** (🚫, confirmado con el usuario antes de ejecutar): probado sobre `test_mcp.elk`, sin `extended_mode`. `{"success": true}`. Verificado en disco: todos los archivos internos de `test_mcp.edb` se reescribieron (timestamps actualizados), `Article.eod` pasó de 985 KB a 525 KB — consistente con una compactación/desfragmentación real de la base de datos del proyecto, no un no-op. **Qué hace exactamente (doc oficial):** "When you reorganize a project, then deleted data is irretrievably removed from the project database. The project database is reduced in size." — es decir, purga de forma **irrecuperable** los datos que ya estaban marcados como borrados dentro del proyecto (artículos/info no usada), no toca datos activos. Explica la reducción de tamaño observada. **`CORRECTPROJECTITEMS`**: diseñado para corregir, sincronizar y reparar automáticamente elementos y datos inconsistentes dentro de un proyecto. (🚫, confirmado): probado sobre el mismo `test_mcp.elk` con `scheme="Default"` (valor de ejemplo de la doc oficial). `{"success": true}`, pero **no tocó ningún archivo** — timestamps y tamaños de `test_mcp.edb` idénticos a los de justo después del `REORGANIZE` anterior. No se puede distinguir "no había nada que corregir" de "el scheme no aplicó nada" — mismo patrón de duda que el falso positivo de `READPROJECTINFO`: `success:true` no implica que se haya hecho un cambio real. **`LOADDIRECTORY`**: escanea un directorio en busca de proyectos EPLAN y los registra en el Project Manager (no modifica los proyectos en sí). Probado con `projects_directory` apuntando a la carpeta de pruebas, `scan_subdirectories=0` → `{"success": true}`. **Los 8 `type` de `project_management` quedan cubiertos** (2026-07-13). **`PUBLISHSMARTPRODUCTION`**: confirmado, publica el proyecto para EPLAN Smart Production (datos de fabricación). Al igual que `CREATESNAPSHOTCOPY`, ignora la extensión dada en `filename` y añade la suya propia: probado con `filename="smartprod_test"` (sin extensión) → generó `smartprod_test.epdz` (archivo 7-zip real, 2.28 MB) — formato propio de Smart Production, no XML. Solo lectura sobre el proyecto (no debería modificarlo, solo exporta). |
| `upgrade_projects` | `XPrjActionUpgradeProjects` | `project_path` **o** `folder_path` (mutuamente excluyentes, tras fix) | ✅ | Confirmado 2026-07-13. 🚫 Irreversible sobre la versión de esquema del proyecto — confirmar antes. **Bug encontrado y corregido**: la firma original (`project_paths: List[str]`) generaba `/PROJECT1:"..."`, `/PROJECT2:"..."` (copiando el patrón numerado de otras acciones como `PAGENAME1`/`TEMPLATE1`) — probado con 1 y con 2 proyectos, **ambos fallan en silencio** (`{"success": false}`, sin mensaje de error, `eplan_status.last_error` tampoco se puebla). La doc oficial solo define el parámetro singular `Project`; probado vía `execute_raw_action` con `/Project:"..."` → `{"success": true}`. Reescrita la función para aceptar `project_path` (un solo proyecto) o `folder_path` (usa `/Folder:"..."`, sube recursivamente todos los proyectos de la carpeta — confirmado funcionando por el usuario). El wrapper nunca soportó de verdad múltiples proyectos en una sola llamada; ahora es explícito en vez de fallar silenciosamente. **Gap que persiste**: el resultado solo hace eco de `success`+parámetros, no devuelve el mensaje real de EPLAN por proyecto (la doc dice que cada proyecto genera un system message, pero se queda en la ventana de mensajes de la GUI) — si `success:false`, no hay forma de saber la causa desde el MCP. |
| `compress_project` | `compress` | `project_name` | ✅ | Confirmado 2026-07-13 sobre `test_mcp.elk`. `{"success": true}`. Verificado en disco: los archivos internos de `test_mcp.edb` (`Article.eod`, `ArticleRef.eod`, `Blob.eod`, etc.) tienen timestamp actualizado al momento de la llamada; `.elk` (stub) sin cambios. **Qué hace realmente (aclarado por Christian):** elimina macros y artículos (piezas) que no se usan en ningún sitio del proyecto — es una limpieza de datos embebidos no usados, no una reorganización/desfragmentación genérica de la base de datos (eso es lo que hace `REORGANIZE` en `project_management`, ver más arriba) ni un empaquetado tipo zip. No está en `action_validation_report.md` (la acción es `compress`, minúscula, no un `X*Action` de la lista validada contra la doc oficial) — no se confirmó contra documentación oficial de EPLAN, solo por comportamiento observado. Sin `project_name`, comportamiento no probado (¿actúa sobre el proyecto con foco, como `close_project`? — sin verificar). No marcada 🚫 en la tabla pero modifica archivos del proyecto; se trató con la misma cautela. **Nota aparte de esta sesión**: durante la prueba, EPLAN se reinició y `eplan_connect`/`eplan_servers` no detectaron el puerto real (escuchaba en 49153, no el 49152 por defecto) — se corrigió el docstring de `eplan_connect`/`eplan_servers` en `server.py` para que la LLM sepa pedir al usuario el puerto real en vez de asumir que EPLAN no está abierto. |
| `synchronize_project` | `synchronize` | `project_name`, `type` (`MULTILINE`\|`SINGLELINE`\|`OVERVIEW`\|`SYSTEMPARTSTOPROJECT`\|`PARTSTOSYSTEM`), `store_mode` (solo con `SYSTEMPARTSTOPROJECT`) | ✅ | Confirmado 2026-07-13 sobre `test_mcp.elk`. **Qué hace (aclarado por Christian)**: sincroniza la base de datos de piezas de EPLAN con la base de datos del proyecto. Firma original solo exponía `project_name`, sin `TYPE` (parámetro requerido en la práctica) ni `STOREMODE` — corregido en `project.py`. **`PARTSTOSYSTEM`** (proyecto → sistema, compartido): probado → `{"success": false}` sin ningún diagnóstico ni en el resultado ni en `eplan_status.last_error`; la causa real solo apareció en la ventana de mensajes de la GUI de EPLAN: **"La base de datos está protegida contra escritura"**. Mismo patrón de fallo silencioso que `upgrade_projects`/`project_management`. Documentado en el docstring para que la LLM sepa que un fallo aquí probablemente es la base de datos de sistema bloqueada/protegida, y que hay que mirar la ventana de mensajes de EPLAN. **`SYSTEMPARTSTOPROJECT`** (sistema → proyecto): probado → `{"success": true}`, no afectado por la protección de escritura (escribe en el proyecto, no en el sistema). `MULTILINE`/`SINGLELINE`/`OVERVIEW` no probados individualmente — dados por buenos según criterio de Christian (mismo mecanismo de acción ya validado con los dos TYPE de mayor riesgo). `project_name` obligatorio en la práctica pese a figurar opcional en la doc — si se omite invocando desde fuera de la GUI (como hace este MCP siempre) lanza excepción en vez de usar el proyecto con foco. |
| `get_current_project` | `selectionset /TYPE:PROJECT` | — | ✅ | Confirmado 2026-07-13 con la tool unificada. Devuelve `{"success": true, "parameters": {"PROJECT": "<ruta completa .elk>", "TYPE": "PROJECT"}}`. Puramente informativo, no modifica nada; consulta el selection set activo de tipo PROJECT (qué proyecto está abierto/activo en el foreground). |
| `set_project_language` | `SetProjectLanguage` | `display` (req), `project_name`, `variable`, `source` | ✅ | Confirmado 2026-07-13. **Bug encontrado y corregido**: la firma original usaba `LANGUAGE` y `READWRITE`, ninguno de los dos existe en la doc oficial de `SetProjectLanguage` (coincide con el `WARN` ya detectado en `action_validation_report.md:19` sobre `READWRITE`) — parámetros reales son `DISPLAY` (idioma(s) mostrados, requerido), `VARIABLE` (idioma activo, por ID o índice posicional tipo `"03_03"`) y `SOURCE` (idioma fuente). Reescrita la función completa. Probado `display="en_US"` sobre `test_mcp.elk` (proyecto ya abierto, requisito de la doc) → `{"success": true}`. **No traduce texto**, solo cambia qué idioma(s) se muestran/cuál está activo — aclarado en el docstring. `variable`/`source` sin probar individualmente. |
| `switch_project_type` | `SwitchProjectType` | `project_type` (req, `1`=Schematic\|`2`=Macro), `project_name` | ✅ | Confirmado 2026-07-13. 🚫 Cambia el tipo de proyecto — confirmado antes. EPLAN solo tiene dos tipos de proyecto: `1`=Schematic project, `2`=Macro project. Docstring original no explicaba los valores — corregido. **Comportamiento del "optional" de la doc oficial no es seguro**: si se omite `PROJECTTYPE`, la acción no hace no-op, sino que **alterna** al tipo que el proyecto no tiene actualmente; si se omite `PROJECTNAME` fuera de la GUI (como siempre hace este MCP) lanza excepción en vez de usar el proyecto con foco — documentado para que la LLM siempre pida ambos explícitamente. Probado `project_type=1` (Schematic) sobre `test_mcp.elk` → `{"success": true}`. |

## `workspace.py`

> ⚠️ El propio código advierte: necesitan mainframe/GUI. Bajo QuietMode remoto pueden no comportarse igual que en modo interactivo (`llm.md` §6 lo marca como gotcha conocido).

| Tool | Acción EPLAN | Parámetros MCP | Estado | Notas |
|---|---|---|---|---|
| `open_workspace` | `OpenWorkspaceAction` | `workspace_name` (req), `silent` | ✅ | Confirmado 2026-07-13 con `workspace_name="Estándar"`, `silent` por defecto (`false`). `{"success": true, "parameters": {"Workspacename": "Estándar", "Silent": "0"}}` — cambio de layout confirmado visualmente por Christian en la GUI. **Qué es un "workspace" (aclarado por Christian)**: el layout físico de ventanas/paneles de EPLAN, el "frontend" visual guardado — no toca datos del proyecto. **Gap corregido**: el docstring original sugería usar `workspace_name="?"` para autodescubrir nombres sin preguntar al usuario; Christian corrigió el criterio — la LLM debe **preguntar siempre** el nombre exacto del workspace al usuario en vez de adivinar o listar por su cuenta, porque los nombres son específicos de cada usuario/instalación (ej. "Estándar" no es un nombre garantizado en otras instalaciones). Docstring de `open_workspace` en `workspace.py` actualizado en consecuencia. `"?"` como mecanismo de listado no se probó — queda sin verificar si funciona, pero ya no se recomienda como flujo por defecto. |
| `save_workspace` | `SaveWorkspaceAction` | `workspace_name` (req), `silent` | ✅ | Confirmado 2026-07-13 con `workspace_name="Test_mcp"` (nombre nuevo, no existente, para no sobrescribir "Estándar"). `{"success": true, "parameters": {"Workspacename": "Test_mcp", "Silent": "0"}}`. 🚫 confirmado antes de ejecutar. **Comportamiento (según doc del propio Action)**: upsert — si el nombre no existe lo crea, si existe lo **sobrescribe sin aviso**. **Gap corregido**: docstring de `save_workspace` en `workspace.py` actualizado para aclarar que un workspace es el layout/lienzo visual de la GUI ("frontend"), no datos de proyecto, y para instruir a la LLM a preguntar siempre el nombre exacto al usuario antes de guardar — nunca adivinar ni reutilizar un nombre visto antes sin confirmar que el usuario quiere sobrescribirlo. |
| `clean_workspace` | `CleanWorkspaceAction` | `workspace_name` (si se omite, limpia el `LAST_USED`), `silent` | ✅ | Confirmado 2026-07-13 con `workspace_name="Test_mcp"` (el creado en la prueba de `save_workspace`, para no arriesgar "Estándar"). `{"success": true, "parameters": {"Workspacename": "Test_mcp", "Silent": "0"}}`. 🚫 confirmado antes de ejecutar. **Qué hace "clean" (aclarado por Christian)**: **elimina el workspace por completo**, no lo resetea/vacía dejándolo existente. **Gap corregido**: docstring de `clean_workspace` en `workspace.py` actualizado para dejar explícito que es destructivo (borra, no limpia en el sentido de "vaciar"), y para instruir a la LLM a preguntar siempre el nombre exacto al usuario — **nunca omitir `workspace_name`**, porque si se deja en blanco actúa sobre el `LAST_USED` implícito de EPLAN, que la LLM no puede consultar ni confirmar de antemano (mismo patrón de riesgo que `close_project`, que actúa sobre lo que tiene foco sin forma de verificarlo vía API). |

## `backup.py`

| Tool | Acción EPLAN | Parámetros MCP | Estado | Notas |
|---|---|---|---|---|
| `backup_project` | `backup /TYPE:PROJECT` | `destination_path` (req), `archive_name` (req), `project_name`, `comment`, `auto_copy_ref_data`, `include_ext_docs`, `include_images`, `backup_method` (`BACKUP`\|`SOURCEOUT`\|`ARCHIVE`) | ✅ | Confirmado 2026-07-13 con `project_name="test_mcp.elk"`, `destination_path="smartprod_test"` (subcarpeta de pruebas), `archive_name="test_mcp_backup.zw1"`, `backup_method` por defecto (`BACKUP`, el método seguro). `{"success": true, ...}` — **verificado en disco** (no solo confiado en el `success:true`, patrón de falso positivo ya visto en el grupo): `test_mcp_backup.zw1` se creó realmente, 14.6 MB, timestamp coincidente con la llamada. **A diferencia de `CREATESNAPSHOTCOPY`** (en `project_management`, que ignora la extensión dada y añade la suya), aquí `archive_name` se respeta tal cual, extensión incluida — consistente con los ejemplos de la doc oficial (`.zw1`). **Gaps corregidos en el docstring de `backup_project`** (`backup.py`), basados en doc oficial aportada por Christian: (1) `project_name` no es realmente opcional fuera de la GUI — la doc dice que solo se omite si se llama desde la GUI con selección activa; este MCP llama como si fuera línea de comandos, así que la LLM debe preguntarlo siempre explícito; (2) nombres de archivo tipo `<nombre>.nnn` (n=dígito) están prohibidos por EPLAN (reservados para partes de un backup dividido); (3) `backup_method="SOURCEOUT"` ("project is filed off") no se probó — se documentó como sospechoso de tener efectos destructivos/irreversibles sobre el proyecto origen (a diferencia de `BACKUP`, que es una copia simple) y se instruyó a la LLM a confirmar con el usuario antes de usarlo. **`SOURCEOUT` probado 2026-07-13** (a petición de Christian, por curiosidad): confirma la sospecha — `{"success": true}`, y en disco el proyecto origen `test_mcp.elk` (36 bytes) **desapareció, renombrado a `test_mcp.els`**, con `test_mcp.edb` también reescrito (timestamp actualizado). Es decir, "filed off" = EPLAN convierte el proyecto de su estado normal `.elk` (escribible) a `.els` (**protegido contra escritura**, aclarado por Christian) — no es un backup adicional inofensivo, es una mutación real sobre el proyecto origen que lo deja read-only. Docstring de `backup_project` actualizado de "sospecha" a "confirmado destructivo", instruyendo a la LLM a **advertir explícitamente al usuario** antes de usar `SOURCEOUT` que el proyecto origen quedará protegido contra escritura (`.els`), y pedir confirmación antes — nunca usarlo por defecto ni en silencio. `ARCHIVE` sigue sin probar. |
| `backup_masterdata` | `backup /TYPE:MASTERDATA` | `destination_path` (req), `archive_name` (req), `source_path` (req), `md_type` (req: `SYMBOLS`\|`MACROS`\|`FORMS`\|`ARTICLES`\|`LANGUAGES`\|`STANDARDSHEET`\|`STATIONDATA`), `filename` (default `*.*`), `comment` | ✅ | Confirmado 2026-07-13, dos pruebas con `md_type="MACROS"`, mismo `destination_path="smartprod_test\masterdata"` (subcarpeta de pruebas). **Prueba 1**: `source_path="Macros"` (carpeta raíz de macros), `archive_name="test_mcp_macros"` → `{"success": true}`, pero el `.zw5` resultante era de solo **503 bytes** — sospechosamente pequeño. **Prueba 2**: `source_path="Macros\ProductoSTD\ELEC\1XDM_v.1.xx.xx"` (carpeta específica con macros reales), `archive_name="test_macro_2"` → `.zw5` de **129,136 bytes**. **Hallazgo clave (aclarado por Christian)**: `source_path` **no recorre subcarpetas** — debe apuntar directamente a la carpeta que contiene los archivos de macro, no a una carpeta raíz/categoría; si se aputna a un nivel superior sin archivos directos, el backup sale casi vacío sin avisar de ningún error. **Gap corregido**: docstring de `backup_masterdata` en `backup.py` actualizado para instruir a la LLM a pedir siempre la carpeta específica (no una carpeta padre/categoría) y a tratar un archivo resultante sospechosamente pequeño como señal de `source_path` demasiado superficial. **Hallazgos adicionales**: (1) si `archive_name` no lleva extensión, EPLAN añade la suya propia (`.zw5` observado) — a diferencia de `backup_project`, que respeta la extensión dada; (2) el backup deja también archivos sueltos directamente en `destination_path`, fuera del `.zw5`: `_BAKINFO.XML`, `_COMMENT.TXT`, y copias de archivos catálogo como `Macros.mlk`/`Macros_2025.mlk` con sus timestamps originales del origen preservados — comportamiento normal de la acción, no basura dejada por error. `pathmap_get_common_paths` no se usó finalmente (Christian dio la ruta directa). |
| `restore_project` | `restore /TYPE:PROJECT` | `archive_name` (req), `project_name` (req), `unpack_project`, `restore_project_info` (default `True`) | ✅ | Confirmado 2026-07-13 con `archive_name="smartprod_test\test_mcp_backup.zw1"`, `project_name="smartprod_test\test_mcp_backup.elk"` (misma carpeta que el archivo, a petición de Christian), `unpack_project=False`, `restore_project_info=False` (mode=0). `{"success": true}` — **verificado en disco**: se generó `test_mcp_backup.elk` (52 bytes) + `test_mcp_backup.edb` real, timestamp coincidente con la llamada. Funciona igual restaurando dentro de la misma carpeta que contiene el `.zw1` origen, sin conflicto (a diferencia de `restore_masterdata`, ver fila siguiente). **Doc oficial aportada por Christian aclaró varios parámetros** (`DESTINATIONPATH` solo aplica a `TYPE:MASTERDATA`, no a `PROJECT`; `UNPACKPROJECT` es para reabrir proyectos previamente empaquetados/archivados, ej. por `SOURCEOUT`/`ARCHIVE` de `backup_project`, no para "descomprimir el backup en sí"; `MODE`/`restore_project_info` controla si se restaura `ProjectInfo.xml`). Docstring de `restore_project` en `backup.py` actualizado con estas aclaraciones e instrucción explícita a la LLM de preguntar siempre `archive_name`/`project_name` sin adivinar. `unpack_project=True` (revertir un `.els` de `SOURCEOUT` a `.elk`) queda sin probar. |
| `restore_masterdata` | `restore /TYPE:MASTERDATA` | `archive_name` (req), `destination_path` (req) | ✅ | Confirmado 2026-07-13 con `archive_name="...\masterdata\test_macro_2.zw5"` y `destination_path` = la misma carpeta que contiene el archivo (`...\masterdata`, a petición de Christian). **No es un bug** (aclarado por Christian) — `restore` sobrescribe `destination_path` como comportamiento esperado; el matiz es que ese overwrite se llevó por delante archivos hermanos no relacionados que estaban en la misma carpeta (`test_mcp_macros.zw5`, `_BAKINFO.XML`, `_COMMENT.TXT`, `Macros.mlk`, `Macros_2025.mlk`), y el resultado reportado fue `{"success": false}` sin mensaje pese a que el overwrite sí ocurrió — el flag `success` no es fiable como señal de "no se tocó nada". **Gap corregido**: docstring de `restore_masterdata` en `backup.py` actualizado para que la LLM advierta siempre al usuario, antes de ejecutar, que `destination_path` va a ser sobrescrito (archivos existentes ahí pueden reemplazarse/eliminarse) y confirme la ruta exacta — nunca asumir que una carpeta con otros archivos es un destino seguro. |

## Contexto que le falta a la LLM en este grupo

- **Nombres de scheme para `project_management` (`scheme`)** — no hay tool para listarlos.
  Candidato de investigación: si `TYPE:READPROJECTINFO` es read-only, probarlo primero
  sin `scheme` para ver qué información expone antes de tocar `REORGANIZE`/`CORRECTPROJECTITEMS`.
- **`open_workspace`**: los nombres de workspace son específicos del usuario/instalación
  (layout de GUI, no dato de proyecto) — la LLM debe preguntar siempre el nombre exacto
  en vez de usar `workspace_name="?"` para autodescubrir o asumir un nombre visto antes.
