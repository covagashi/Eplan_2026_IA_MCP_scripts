# 01 — Proyecto, Workspace, Backup/Restore

Ver leyenda de estado en [00-INDEX.md](00-INDEX.md).

## `project.py`

| Tool | Acción EPLAN | Parámetros MCP | Estado | Notas |
|---|---|---|---|---|
| `open_project` | `ProjectOpen` | `project_path` (req), `open_mode` (`Standard`\|`ReadOnly`\|`Exclusive`) | ✅ | Confirmado 2026-07-13 con `project_path="D:\Archivos\Downloads\Nuevo proyecto.elk"`, sin `open_mode`. Resultado: `{"success": true, "parameters": {"PROJECT": "...", "Project": "..."}}` (nótese que devuelve la clave duplicada en dos capitalizaciones distintas, `PROJECT` y `Project`). **Gap de contexto encontrado y corregido**: el docstring original no advertía a la LLM contra adivinar/reutilizar una ruta de proyecto vista previamente en la conversación; se añadió una instrucción explícita en `project.py` (`open_project`) para que siempre pregunte al usuario la ruta salvo que ya se la haya dado en ese turno. |
| `close_project` | `XPrjActionProjectClose` | — | ✅ | Confirmado 2026-07-13. **Hallazgo clave**: la acción no acepta nombre/ruta de proyecto — cierra el que tenga el **foco de ventana** en la GUI de EPLAN en ese instante (el último que el usuario clicó), no uno elegible por API. Se probó `selectionset /TYPE:PROJECTS` (plural) y `/TYPE:PROJECT` (singular) vía `execute_custom_script` esperando que `PROJECTS` listara todos los proyectos abiertos: en la práctica **siempre devuelven el mismo único valor**, reflejando solo el proyecto con foco actual — cambia con solo hacer clic en otra ventana, dos ejecuciones seguidas del mismo script devolvieron dos proyectos distintos sin que la LLM hiciera nada entremedias. No existe ninguna acción conocida que enumere "todos los proyectos abiertos". Un intento de cerrar `Nuevo proyecto.elk` explícitamente cerró `Macros_7C0x_v.7.13.00.elk` en su lugar porque ese tenía el foco. **Gap corregido**: docstring de `close_project` documenta esta limitación explícitamente y instruye a la LLM a pedir confirmación de cuál tiene el foco antes de cerrar si hay más de un proyecto abierto. No se verificó qué pasa con cambios sin guardar — sigue siendo una incógnita, ver `llm.md` §7. |
| `project_management` | `projectmanagement` | `type` (req: `READPROJECTINFO`\|`PUBLISHSMARTPRODUCTION`\|`CREATESNAPSHOTCOPY`\|`EXPORTPROPERTYPLACEMENTSSCHEMAS`\|`IMPORTPROPERTYPLACEMENTSSCHEMAS`\|`REORGANIZE`\|`CORRECTPROJECTITEMS`\|`LOADDIRECTORY`), `project_name`, `filename`, `scheme`, `overwrite`, `extended_mode`, `projects_directory`, `scan_subdirectories` | 🟡 | Confirmado 2026-07-13. **`READPROJECTINFO` NO es de solo lectura** (a pesar del nombre) — la doc oficial dice que "carga info de un XML *hacia* el proyecto", es un import y requiere `filename` (XML de origen). Probado sin `filename` → `{"success": false}` sin mensaje (gap de excepciones tragadas, ver `TODO.md`). **Reprobado con `filename` apuntando a un XML del formato equivocado** (`PPSchemas.xml`, que es `PropertyView`, no `ProjectInfo`) → devolvió `{"success": true}`. Esto es un hallazgo más grave: `success:true` **no garantiza que la operación haya hecho algo válido** — con un XML de esquema incorrecto, pudo haber importado datos parciales/basura o no haber hecho nada, y de cualquier forma el wrapper reporta éxito igual. No verificado qué cambió realmente en el proyecto tras esto (no había snapshot previo con el que comparar) — **posible riesgo de corrupción silenciosa de datos si se usa con un XML mal formado sin darse cuenta**. **`CREATESNAPSHOTCOPY` = crear una copia completa de un proyecto.** Uso real: `type="CREATESNAPSHOTCOPY"`, `project_name=<ruta .elk origen>`, `filename=<ruta destino>` — **la extensión de `filename` es irrelevante, EPLAN siempre añade `.elk`+`.edb` propios sobre el nombre base**, así que no hace falta (ni tiene sentido) ponerle ninguna extensión; mejor pasar solo el nombre base, ej. `"D:\Archivos\Downloads\test"`. Confirmado con `filename="D:\Archivos\Downloads\test.xml"` (extensión de prueba, innecesaria) → generó `test.xml.elk` (36 bytes, stub) + `test.xml.edb` (carpeta, base de datos real) — la `.xml` quedó absorbida dentro del nombre base, no descartada. `scheme` sigue sin forma de listarse sin GUI — mismo problema que `CONFIGSCHEME` en otras acciones. **`EXPORTPROPERTYPLACEMENTSSCHEMAS`**: confirmado, exporta de verdad a un XML real (aquí sí la extensión `.xml` es correcta y se respeta, a diferencia de `CREATESNAPSHOTCOPY`). Genuinamente de solo lectura sobre el proyecto. Probado con `filename="D:\Archivos\Downloads\PPSchemas.xml"` → generó XML de 69 KB válido (`<EplanPxfRoot Type="PropertyView" ... SourceProject="...">`, 31 objetos). **Emparejamiento real de ida/vuelta**: `EXPORTPROPERTYPLACEMENTSSCHEMAS` ↔ `IMPORTPROPERTYPLACEMENTSSCHEMAS` — mismo esquema (`Type="PropertyView"`), no confundir con `READPROJECTINFO` que espera un XML de "ProjectInfo" distinto. Si al usuario le falta el XML de property placement schemes, se genera con `EXPORTPROPERTYPLACEMENTSSCHEMAS` sobre cualquier proyecto que ya lo tenga configurado. **`IMPORTPROPERTYPLACEMENTSSCHEMAS`**: confirmado con ciclo completo real — se exportó `PPSchemas.xml` desde `Macros_7C0x` y se importó con éxito en `D:\Archivos\Downloads\test_mcp.elk` (`overwrite=1`). `{"success": true}` con formato correcto — a diferencia del falso positivo de `READPROJECTINFO`, aquí sí hay confianza en el resultado porque el ciclo export→import usa el mismo esquema. Nota: dentro de `test_mcp.edb` existe un `ProjectInfo.xml` real (47.8 KB) — candidato a probar como `filename` correcto para `READPROJECTINFO` en una vuelta futura a esa acción. **`REORGANIZE`** (🚫, confirmado con el usuario antes de ejecutar): probado sobre `D:\Archivos\Downloads\test_mcp.elk`, sin `extended_mode`. `{"success": true}`. Verificado en disco: todos los archivos internos de `test_mcp.edb` se reescribieron (timestamps actualizados), `Article.eod` pasó de 985 KB a 525 KB — consistente con una compactación/desfragmentación real de la base de datos del proyecto, no un no-op. **Qué hace exactamente (doc oficial):** "When you reorganize a project, then deleted data is irretrievably removed from the project database. The project database is reduced in size." — es decir, purga de forma **irrecuperable** los datos que ya estaban marcados como borrados dentro del proyecto (artículos/info no usada), no toca datos activos. Explica la reducción de tamaño observada. **`CORRECTPROJECTITEMS`**: diseñado para corregir, sincronizar y reparar automáticamente elementos y datos inconsistentes dentro de un proyecto. (🚫, confirmado): probado sobre el mismo `test_mcp.elk` con `scheme="Default"` (valor de ejemplo de la doc oficial). `{"success": true}`, pero **no tocó ningún archivo** — timestamps y tamaños de `test_mcp.edb` idénticos a los de justo después del `REORGANIZE` anterior. No se puede distinguir "no había nada que corregir" de "el scheme no aplicó nada" — mismo patrón de duda que el falso positivo de `READPROJECTINFO`: `success:true` no implica que se haya hecho un cambio real. **`LOADDIRECTORY`**: escanea un directorio en busca de proyectos EPLAN y los registra en el Project Manager (no modifica los proyectos en sí). Probado con `projects_directory="D:\Archivos\Downloads"`, `scan_subdirectories=0` → `{"success": true}`. **Los 8 `type` de `project_management` quedan cubiertos** (2026-07-13). **`PUBLISHSMARTPRODUCTION`**: confirmado, publica el proyecto para EPLAN Smart Production (datos de fabricación). Al igual que `CREATESNAPSHOTCOPY`, ignora la extensión dada en `filename` y añade la suya propia: probado con `filename="D:\Archivos\Downloads\smartprod_test"` (sin extensión) → generó `smartprod_test.epdz` (archivo 7-zip real, 2.28 MB) — formato propio de Smart Production, no XML. Solo lectura sobre el proyecto (no debería modificarlo, solo exporta). |
| `upgrade_projects` | `XPrjActionUpgradeProjects` | `project_paths` (lista) | ⬜ | 🚫 Irreversible sobre la versión de esquema del proyecto — confirmar antes. |
| `compress_project` | `compress` | `project_name` | ⬜ | — |
| `synchronize_project` | `synchronize` | `project_name` | ⬜ | — |
| `get_current_project` | `selectionset /TYPE:PROJECT` | — | ✅ | Confirmado 2026-07-13 con la tool unificada. Devuelve `{"success": true, "parameters": {"PROJECT": "<ruta completa .elk>", "TYPE": "PROJECT"}}`. Puramente informativo, no modifica nada; consulta el selection set activo de tipo PROJECT (qué proyecto está abierto/activo en el foreground). |
| `set_project_language` | `SetProjectLanguage` | `language` (req), `project_name`, `read_write` (default `True`) | ⬜ | Ojo: mismo tipo de parámetro `LANGUAGE` que falló en `label` por venir vacío — aquí es obligatorio en la firma Python así que no debería repetirse el problema. |
| `switch_project_type` | `SwitchProjectType` | `project_type` (req), `project_name` | ⬜ | 🚫 Cambia el tipo de proyecto — confirmar antes. |

## `workspace.py`

> ⚠️ El propio código advierte: necesitan mainframe/GUI. Bajo QuietMode remoto pueden no comportarse igual que en modo interactivo (`llm.md` §6 lo marca como gotcha conocido).

| Tool | Acción EPLAN | Parámetros MCP | Estado | Notas |
|---|---|---|---|---|
| `open_workspace` | `OpenWorkspaceAction` | `workspace_name` (req; **`"?"` lista todos los workspaces disponibles** — útil para descubrir nombres sin preguntar al usuario), `silent` | ⬜ | Probar primero con `workspace_name="?"` para inventariar antes de abrir uno real. |
| `save_workspace` | `SaveWorkspaceAction` | `workspace_name` (req), `silent` | ⬜ | 🚫 Sobrescribe/crea workspace — confirmar nombre con el usuario si no es evidente cuál. |
| `clean_workspace` | `CleanWorkspaceAction` | `workspace_name` (si se omite, limpia el `LAST_USED`), `silent` | ⬜ | 🚫 Destructivo sobre el workspace — confirmar antes, y **nunca omitir `workspace_name`** sin saber cuál es el "last used" real. |

## `backup.py`

| Tool | Acción EPLAN | Parámetros MCP | Estado | Notas |
|---|---|---|---|---|
| `backup_project` | `backup /TYPE:PROJECT` | `destination_path` (req), `archive_name` (req), `project_name`, `comment`, `auto_copy_ref_data`, `include_ext_docs`, `include_images`, `backup_method` (`BACKUP`\|`SOURCEOUT`\|`ARCHIVE`) | ⬜ | — |
| `backup_masterdata` | `backup /TYPE:MASTERDATA` | `destination_path` (req), `archive_name` (req), `source_path` (req), `md_type` (req: `SYMBOLS`\|`MACROS`\|`FORMS`\|`ARTICLES`\|`LANGUAGES`\|`STANDARDSHEET`\|`STATIONDATA`), `filename` (default `*.*`), `comment` | ⬜ | `source_path` — usar `pathmap_get_common_paths` primero para obtener `$(MD_SYMBOLS)`, `$(MD_FORMS)`, etc. en vez de adivinar la ruta. |
| `restore_project` | `restore /TYPE:PROJECT` | `archive_name` (req), `project_name` (req), `unpack_project`, `restore_project_info` (default `True`) | ⬜ | 🚫 Sobrescribe destino — confirmar antes (`llm.md` §7: "restore/backup overwrites"). |
| `restore_masterdata` | `restore /TYPE:MASTERDATA` | `archive_name` (req), `destination_path` (req) | ⬜ | 🚫 Igual que arriba, confirmar antes. |

## Contexto que le falta a la LLM en este grupo

- **Nombres de scheme para `project_management` (`scheme`)** — no hay tool para listarlos.
  Candidato de investigación: si `TYPE:READPROJECTINFO` es read-only, probarlo primero
  sin `scheme` para ver qué información expone antes de tocar `REORGANIZE`/`CORRECTPROJECTITEMS`.
- **`open_workspace(workspace_name="?")`** es la única acción de este grupo que
  documenta explícitamente su propio mecanismo de auto-descubrimiento — usarlo antes
  de pedirle el nombre al usuario.
