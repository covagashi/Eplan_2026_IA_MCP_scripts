# EPLAN EEC Pro RAG MCP Server (Cloudflare)

Remote MCP server that allows Claude to search the **EPLAN EEC Pro 2026** documentation using semantic search with vectors.

Deployed on Cloudflare Workers with Vectorize + Workers AI.

> This is the EEC Pro counterpart of [`cloudflare_rag/`](../cloudflare_rag/) (which serves Eplan P8 docs).

## Architecture

```
Claude Code  -->  MCP (Streamable HTTP)  -->  Cloudflare Worker (eecpro-rag-mcp)
                                                   |
                                              Workers AI (bge-base-en-v1.5)
                                                   |
                                              Vectorize (eecpro-knowledge-base)
                                              ~3,043 vectors / 768 dims / cosine
```

## Data Source

- **ChromaDB local:** `Eplan_EEC_PRO/rag_db_llama_chroma/`
- **Documents:** 1,648 markdown pages in 36 categories
- **Indexed with:** LlamaIndex (MarkdownNodeParser + SentenceSplitter)
- **Embedding model:** BAAI/bge-base-en-v1.5

## Migration Pipeline

```
1. python migration/scripts/01_verify_and_export.py  # Verify + export NDJSON
2. cd migration && bash scripts/02_deploy_to_cloudflare.sh  # Deploy to Cloudflare
```

## Instalacion del MCP en Claude Code

### via mcp-remote

```bash
claude mcp add eecpro-rag -- cmd /c npx mcp-remote https://rageecpro.covaga.xyz/mcp
```

### Verificar

```bash
claude mcp list
```

Should show `eecpro-rag` in the list. Then inside Claude Code:

```
/mcp
```

Should show `eecpro-rag` connected with 2 tools available.

## Tools available

| Tool | Description |
|------|-------------|
| `eecpro_search` | Semantic search in EEC Pro documentation. Supports filtering by category (36 categories: refformulas, eececad, eecplc, eecscripting, admin, refcommands, etc.) |
| `eecpro_stats` | Statistics of the vector index (number of vectors, dimensions, metric) |

## EEC Pro Categories

| Category | Description |
|----------|-------------|
| `refformulas` | Formula language (317 pages) |
| `admin` | Installation, configuration, VMArgs (304 pages) |
| `eecbase` | Base functionality (119 pages) |
| `refformui` | Form-UI reference (84 pages) |
| `refcommands` | Commands (79 pages) |
| `refscripting` | Scripting reference (58 pages) |
| `concept` | Concepts (58 pages) |
| `eececad` | ECAD module (47 pages) |
| `eecplc` | PLC module (47 pages) |
| + 27 more | Tutorials, SAP, Office, Graph2D, etc. |

## REST Endpoints

In addition to the MCP, the worker exposes a direct REST API. The base URL is `https://rageecpro.covaga.xyz`.

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/health` | Health check |
| POST | `/search` | Semantic search (body: `{"query": "...", "topK": 5}`) |
| GET | `/stats` | Index statistics |
| POST | `/add-vectors` | Insert vectors (requires `Authorization: Bearer <WORKER_API_KEY>`) |

### Usage Examples

**1. Search using cURL (Windows CMD)**
```cmd
curl -X POST https://rageecpro.covaga.xyz/search -H "Content-Type: application/json" -d "{\"query\": \"job server configuration\", \"topK\": 3, \"category\": \"eecjobserver\"}"
```

**2. Search using Python**
```python
import requests

url = "https://rageecpro.covaga.xyz/search"
payload = {
    "query": "job server configuration",
    "topK": 3,
    "category": "eecjobserver" # Optional filter
}
headers = {"Content-Type": "application/json"}

response = requests.post(url, json=payload, headers=headers)
print(response.json())
```


## Structure

```
cloudflare_rag_eecpro/
├── migration/
│   ├── scripts/
│   │   ├── 01_verify_and_export.py   # Export ChromaDB → NDJSON batches
│   │   └── 02_deploy_to_cloudflare.sh # Create index + upload + deploy
│   └── manifest.json                 # (generated by 01)
├── worker/
│   ├── src/
│   │   └── index.ts                  # Worker (MCP + REST)
│   ├── wrangler.jsonc                # Cloudflare configuration
│   ├── package.json
│   ├── tsconfig.json
│   └── worker-configuration.d.ts
└── README.md                         # This file
```

## Comparison with cloudflare_rag (P8)

| Aspect | `cloudflare_rag` (P8) | `cloudflare_rag_eecpro` (EEC Pro) |
|--------|----------------------|----------------------------------|
| Product | Eplan Electric P8 | Eplan EEC Pro |
| Source DB | `chroma_db_sota/` (870MB, 57K vectors) | `rag_db_llama_chroma/` (102MB, ~3K vectors) |
| Vectorize index | `eplan-knowledge-base` | `eecpro-knowledge-base` |
| Worker name | `eplan-rag-mcp` | `eecpro-rag-mcp` |
| Domain | `rag2026.covaga.xyz` | `rageecpro.covaga.xyz` |
| MCP name | `eplan-rag` | `eecpro-rag` |
| Tools | `eplan_search` / `eplan_stats` | `eecpro_search` / `eecpro_stats` |
| Categories | 3 (API Reference, User Guide, Api) | 36 (refformulas, eececad, eecplc, ...) |
